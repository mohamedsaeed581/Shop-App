import 'package:flutter/material.dart';

class RedisterScreen extends StatelessWidget {
  const RedisterScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
